# ═══════════════════════════════════════════════════════════════════
# docker build 的上下文排除表
# ═══════════════════════════════════════════════════════════════════
# ⚠️ **为什么这个文件在这个项目里特别重要**
#
# 本机 Docker VM 的**内存硬上限是 7.63 GiB**，而当前已有 8 个别的项目的
# 容器在跑、占掉约 6.13 GiB（实测，见对话交接.md §4.5）。
# 构建镜像时 docker 会把整个上下文**打包送进 VM**，上下文越大，
# 打包进程自身占的内存越多 —— 在只剩 1.5 GiB 余量的情况下，
# 这不是「慢一点」的问题，是**可能把构建进程直接 OOM 掉**的问题。
#
# 仓库里最重的三块：
#   · frontend/node_modules   — 数百 MB，且 qy-backend 镜像**根本用不到**
#   · frontend/dist           — 9.7 MB，由 qy-nginx 挂载宿主目录，不进后端镜像
#   · ui参考/                 — 约 44 MB，其中 34 MB 是全友版权摄影（本来也不入库）
#
# 注意：`.dockerignore` 只影响**构建上下文**，不影响 qy-nginx 用
# bind mount 挂载 frontend/dist —— 那条路走的是宿主文件系统，与构建无关。

# ── 版本控制与元数据 ──────────────────────────────────────────
.git
.gitignore
.gitattributes

# ── 前端（完全不进后端镜像）───────────────────────────────────
frontend/node_modules
frontend/dist
frontend/.vite
node_modules

# ── 版权素材与演示素材 ────────────────────────────────────────
# ui参考/ 里 34 MB 是全友官网版权摄影，公开仓库都不让进，
# 更没理由塞进镜像。演示户型图是 3 张自己生成的 PNG，
# 运行时用不到（用户从浏览器上传），但留着体积也小 —— 这里一并排除，
# 需要演示时从宿主机取。
ui参考
演示素材

# ── 测试与开发期产物 ──────────────────────────────────────────
# 测试代码不进运行时镜像（702 个用例是开发期资产）。
tests
conftest.py
pytest.ini
.pytest_cache
__pycache__
**/__pycache__
*.py[cod]
*.egg-info
.mypy_cache
.ruff_cache
.coverage
htmlcov

# ── 密钥（**绝不能进镜像层**）─────────────────────────────────
# 镜像分层是永久的：一旦 .env 被 COPY 进去，即使后来删掉，
# 它仍然留在下层 layer 里，`docker history` 能捞出来。
# 本项目所有配置都走环境变量注入（见 docker-compose.yml 的 environment）。
.env
.env.local
.env.*.local
*.pem
*.key
secrets

# ── 运行时数据与日志（都在 E: 盘，靠 bind mount 进容器）───────
logs
*.log
data
uploads
outputs
chroma

# ── 文档 ──────────────────────────────────────────────────────
# 需求文档是决策记录，是给人看的，不是运行时依赖。
#
# ⚠️⚠️ **这里绝对不要写 `*.md`。**
#    写的时候我差点就用了 `*.md`，那会同时干掉 skills/ 下的 6 份文档
#    （01_layout_parser.md … 06_risk_reviewer.md），而 Dockerfile 里
#    有 `COPY skills/ /app/skills/` —— 结果是一个**静默变空的目录**：
#    构建成功、镜像正常、没有任何报错，只是那份"Agent 规格文档"没了。
#    这正是本项目反复在防的那类错误（不报错，只是东西悄悄少了）。
#    所以按**具体文件**排除，不按扩展名。
#
# ⚠️ 另注意 seed_data/ **不能**排除 —— 材料目录与价目表是运行时真的会读的。
docs
/README.md
/对话交接.md

# ── 其它 ──────────────────────────────────────────────────────
.vscode
.idea
.DS_Store
Thumbs.db
desktop.ini
