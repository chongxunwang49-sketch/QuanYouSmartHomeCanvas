# 对话交接 —— 全友·智绘家 QuanYou Smart HomeCanvas

> **这份文件是写给下一个窗口的 Claude 自己的。**
> 目标不是让人读懂，是让新窗口的"我"能无缝接手：拥有同样的项目认知、
> 同样的判断纪律、同样知道哪些坑踩过、哪些约束不能碰。
>
> 读完之后你应该能直接继续干活，而不需要重新问用户"这个项目是做什么的"。

---

## 0. 三十秒版本

一个**面试用的旗舰项目**：给内江市全友家居做的多模态室内装修推荐系统。
用户上传户型图 → AI 解析出结构化户型 → 诊断 → 生成多套装修方案（风格/预算/材料/避坑）→
渲染 2D 矢量图 + 物品热区 → 生成 3D 场景并支持第一人称漫游。

**当前状态**：核心链路全部打通并实测可用。702 个测试全过。
最近一轮刚做完 3D 第一人称漫游（含可开关的门）。
**下一步候选**：Docker Compose、`/system/metrics`、审计日志、MCP 工具、登录角色、DB 持久化、前端测试。

---

## 1. 项目身份

| 项 | 值 |
|---|---|
| 名称 | 全友·智绘家（QuanYou Smart HomeCanvas） |
| 性质 | **面试旗舰项目**（用户要用它去面试，不是生产系统） |
| 甲方场景 | 内江市全友家居 |
| 官方网址 | https://www.quanyou.com.cn/ |
| 工作目录 | `C:\Users\DELL\Desktop\全友·智绘家QuanYou Smart HomeCanvas` |
| 分支 | `main` |
| 远程 | `gitee` = gitee.com/wcx547379191/quan-you-smart-home-canvas（**主推**）<br>`origin` = github.com/chongxunwang49-sketch/QuanYouSmartHomeCanvas |
| 最新提交 | `cb629af feat(3d): 可开关的门 + 修视野参数 + 修离开页面丢任务` |
| 文档 | `docs/需求/需求文档.md`（**决策记录，不许改**）、`docs/面试/面试亮点.md`（35 章）、`README.md`（423 行） |

**GitHub 最近推不上去**（`Recv failure: Connection was reset` / `Failed to connect to github.com port 443`）——
不是代码问题，是网络。Gitee 正常。推的时候别因为 GitHub 失败就以为提交没成功。

---

## 2. 硬约束（违反 = 严重错误）

这些是用户明确要求过的，**每一条都要继续遵守**：

1. **任何有版权的东西不可以上传公开渠道。**
   - 已落实：`.gitignore` 里排除了 `ui参考/*`（只放行 README/MANIFEST/stitch_ai）
     和 `frontend/src/assets/images/cases/`（14 张全友版权实景图）
   - **新增任何全友相关的图片/素材时，先想一遍这条**

2. **不要修改 `docs/需求/需求文档.md`。** 它是决策记录。
   发现文档与实际不符时，**在代码注释里标注差异**，不要回去改文档。

3. **绝不写真实 API Key。** 用环境变量占位（`${DEEPSEEK_API_KEY}`）。
   不在代码里硬编码 key、模型名、路径。

4. **不许直接 `openai.OpenAI()`。** 所有 LLM 调用必须走 `backend/app/core/llm_client.py` 的 `LLMClient`。

5. **不许编造全友真实产品价格。** 只用 `seed_data/material_catalog.json`（演示数据），
   且必须带 `disclaimer` 展示。

6. **不生成"装修效果图"** —— 交付物叫 **"3D 户型渲染图"**。
   AC-39 要求 UI 文案、README、API 字段里都不出现「装修效果图」。**改文案时注意。**

7. **不把户型图上传给第三方。** `prefer_local=true` 时图像绝不出本机（ADR-12，已抓包验证）。

8. **写任何 UI 页面之前，先问用户要参考文件。**（这条是更早的对话里用户提的要求）

---

## 3. 工作方式与判断纪律（**最重要的一节**）

这部分比技术细节重要得多。前一个窗口之所以能被信任，靠的是下面这些习惯。
**新窗口必须继续这么做。**

### 3.1 语言与语气
- **全程用中文回复用户。**
- 代码注释也用中文，风格见下文。

### 3.2 先量，再断言
**这是这个项目的核心方法论。** 有太多错误是"看起来没问题但实际错了"，唯一能发现的办法是量。

做过的例子：
- 量像素宽高比，才发现条件图把 685×505 的户型拉成了正方形
- 量房房间尺寸，才发现"房间显小"**不是**几何错（算出来 3.99×3.10m vs 图纸 4.0×3.2m），
  而是 Three.js 的 `fov` 是**垂直**视场角、我按水平写了 68°（≈98° 水平，超广角）
- 量 IoU，才把"阈值 0.70 是拍的"从推测变成实测证据（4 个样本全部不达标）
- 量房间连通，才发现固定 0.6m 探针在净宽 0.264m 的走廊里必然打偏

**禁止的说法**：「应该没问题」「看起来对」「按理说」。
要么给数字，要么说"没测"。

### 3.3 变异验证测试（mutation check）
写完测试后，**故意把代码改坏，确认测试会红**。
不能失败的测试比没有测试更糟。

做过的例子：
- 把"切门洞"关掉 → 洪水填充显示两间卧室到不了 ✓
- 把碰撞投影错开 20px → 12 个热区顶点里 10 个落到房间外 ✓
- 把门侧判定换回两版旧算法 → 一版塌成同一条边、一版变自环 ✓
- 去掉 `rec.progress = ...` 那行 → 2/3 个用例变红 ✓

### 3.4 承认错误，明确说"我错了"
这个项目里我犯过多次错，用户明确认可这种处理方式。例子：
- 我曾断言"户型图上传区坏了"，读了 Vue 的 `setRef` 源码后发现 `refs[ref] = value`
  是无条件执行的，`$refs.fileInput` 一直有效 —— **我主动更正并道歉**
- 我曾两次说"我看不到图片，需要你的人眼" —— **这是错的**，Read 工具支持 PNG。
  发现后明确告诉用户之前有些"请你帮我看"的请求是白做的
- 测试挂了时，先判断"是代码错了还是断言错了"。
  有几次**是断言错了**（例：门扇中点 ≠ 模型报的门中心，差 3–8cm，代码是对的）

### 3.5 不许静默降级 / 不许界面说谎
这是需求文档反复强调的原则（AC-17：静默降级 = 欺骗用户）。
- 修过一个真 bug：`prechecking` 阶段是写死的假阶段，代码里根本没有图片质检
- 修过：报价单审查时界面显示"正在生成装修方案…"（措辞错了）
- 所有降级/警告都必须写进 `issues` / `warnings` 并在界面上可见

### 3.6 失败方向要选对
同一个原则在不同场景推出**相反**的默认值，两边都要想清楚：
- `precheck`（图片预检）：检查器自己崩了 → **放行**（不能挡住用户主流程）
- `geo_check`（几何自检）：检查器崩了 → **不给热区**（不能把"没验证"说成"已验证"）

### 3.7 停止点
用户说过：「有什么需要我指导或者决定方案的地方，有风险的地方，出问题的地方再停下」。
- 遇到**方案取舍**（如"第一人称碰撞怎么实现"）→ 停下来问
- 遇到**需要人眼确认的**（3D 观感、画面好不好看）→ 明确说"我看不到，需要你确认"
- 纯执行的事 → 直接做完，别每步都问

### 3.8 代码注释风格（**必须match**）
这个仓库的注释有非常鲜明的风格，**新写的代码要一致**：

- 大段中文块注释，用 `═══` 分隔线
- 标题常常是"为什么 XXX **不是** YYY"
- 用 `⚠️` 标记踩过的坑
- **重点是解释"为什么这么做"和"不这么做会怎样"**，而不是"这行代码做了什么"
- 经常写"实测"数字
- 会写"这个 bug 的特点是不报错，只是功能悄悄变差"

示例（真实片段）：
```python
# ⚠️ **首尾也要焊一次。**
#
# 上面的循环只比较**相邻**点，闭合环的末点与首点不是相邻的，
# 于是差几厘米也不会被合并 —— 而 `is_loop` 要求首尾几乎相等，
# 结果一个肉眼看上去闭合的环被判成开放折线（实测差 1.6cm 就中招）。
```

### 3.9 提交信息风格
- 中文，`type(scope): 标题`
- 正文用 `═══ 小标题 ═══` 分节
- 大量"为什么"和"实测数字"
- **必须**以 `Co-Authored-By: Claude Code <noreply@anthropic.com>` 结尾（系统提醒要求的）

---

## 4. 技术栈与环境（实测数据）

### 4.1 硬件（**实测**，不要再猜）
```
CPU     : Intel i7-11800H  8核16线程
内存    : 16GB (15.7GiB 可用)
GPU     : NVIDIA RTX 3050 Laptop 4GB  +  Intel UHD Graphics（双显卡 Optimus）
驱动    : 576.83
显示器  : 1920×1080，但**逻辑分辨率是 1536×864 → Windows 缩放 125%**
          ⇒ devicePixelRatio = 1.25，WebGL 要多渲 56% 的像素（已做 DPR 封顶）
```

### 4.2 Python 环境（**两个，别用错**）
| 环境 | 路径 | 版本 | torch | CUDA | 用途 |
|---|---|---|---|---|---|
| base | `python`（默认） | 3.13.5 | 2.13.0+cpu | **False** | **跑测试、起后端** |
| pytorch | `D:/AI damoxing/Anaconda3/envs/pytorch/python.exe` | 3.11.15 | 2.13.0+cu126 | **True** | 出图 / 任何要用 GPU 的事 |

**base 环境没有 CUDA** —— 出图在 base 里是不可能的。
但**测试和后端**用 base 就好（README 里写的是 pytorch 环境，实测 base 也能跑）。

### 4.3 其他依赖状态
- **Redis 没起**（`127.0.0.1:6379` 连不上）。所以后端 health 常年是 `degraded`，
  任务进度走内存回退路径。**这条路径是常态不是兜底**，相关代码要按常态对待。
- Chroma 知识库：已建，419 条 chunk，可用
- 材料目录：28 件商品，可用
- DeepSeek：可用

### 4.4 前端
```
Vue 3.5.43 + TS + Vite 6 + Pinia + Vue Router + Tailwind 3.4
Element Plus 2.14.6（**深路径导入**，见下文）
ECharts 5.6（**按需注册**）
three.js 0.186（**动态 import**）
```

**三条性能纪律，破坏了就回到老问题：**
1. Element Plus 必须深路径导入 `element-plus/es/components/message/index.mjs`
   —— 桶式入口摇不掉（每个组件 `index.mjs` 顶层调 `withInstall`，是真副作用），938 KB → 28 KB
2. ECharts 用 `echarts/core` + `use([...])` 按需注册，1036 KB → 456 KB
3. **three.js 必须动态 `import()`**。`rig.ts` / `scene.ts` 都 import three（570KB），
   任何从这两个文件**静态**引常量都会把 three 拉回主包。
   实测踩过：`SceneViewer` 从 `rig.ts` 引了 `DEFAULT_HFOV_DEG` → ParseView 45KB → **437KB**。
   现在 FOV 数学放在 **`src/three/coords.ts`（不 import three）**。

**构建后必须检查 `ParseView-*.js` 是否还在 45–50KB 量级。**

---

## 5. 架构与目录

```
backend/app/
  agents/          A-01..A-06 六个 Agent（base.py 是抽象基类）
  api/             routes.py（HTTP 路由）tasks.py（异步任务+进度）store.py（户型暂存）
  core/            config.py（全部配置）llm_client.py（LLM 唯一入口）redis_client.py
                   capabilities.py（能力守卫）logger.py
  graph/           state.py workflow.py（LangGraph 主图）
  schemas/         layout.py budget.py plan.py material.py risk.py（pydantic 模型）
  services/
    budget/engine.py        规则引擎算钱（AC-05 要求非 LLM）
    geometry/normalize.py   ★ 几何内核：像素户型 → 米制 Scene
    geometry/walkable.py    ★ 可漫游性：碰撞线段/房间连通/出生点
    image/base.py           ImageProvider 抽象 + render_conditioning_image
    image/precheck.py       图片质量预检（AC-27）
    image/local_sd15.py     SD1.5（**已决定不接**，见 7.3）
    knowledge/              Chroma 检索（chunking/retriever/store）
    material/catalog.py     材料目录读取
    render/projection.py    ★ 米↔像素，渲染与热区共用
    render/svg.py           ★ 2D 矢量渲染器
    render/hotspots.py      ★ 物品热区
    render/geo_check.py     AI 图几何一致性自检（AC-28，默认关闭）

frontend/src/
  api/             client.ts（axios 封装）index.ts（接口）types.ts（契约）
  components/      PlanViewer.vue（2D图+热区）SceneViewer.vue（3D漫游）
                   PhaseProgress.vue（进度）AppSidebar/AppHeader/…
  composables/     useTaskPolling.ts（轮询 + 会话记忆）useFrameStats.ts（帧率+降质）
  three/           coords.ts（坐标映射+FOV数学，**不 import three**）
                   rig.ts（第一人称相机+碰撞）scene.ts（建场景+门扇）
  views/           8 个页面
  stores/          task.ts health.ts

演示素材/户型图/     3 张自己生成的演示户型图 + README
scripts/           14 个脚本（见 11.3）
tests/             26 个测试文件，702 个用例
```

---

## 6. 验收标准现状（39 条 AC）

需求文档 `docs/需求/需求文档.md` 第 1762 行起有完整表格。**逐条现状：**

### ✅ 已实现且实测通过
| AC | 内容 | 证据 |
|---|---|---|
| AC-02 | 户型图上传解析 | 实测识别 9 间房，名称/类型/面积全对 |
| AC-03 | 户型诊断 ≥5 维度 | 五维评分 |
| AC-04 | 多方案并行生成 | LangGraph `Send` 扇出 |
| AC-05 | 预算 ≥7 分项，**规则引擎算** | `services/budget/engine.py` |
| AC-06 | 避坑审查 ≥5 类 + 引用知识库 | RAG 检索，引用可溯源 |
| AC-07 | **矢量图 <3s，≥512×512** | 实测 **0.5ms**，1024×1024 |
| AC-09 | 物品热区 + precision 字段 | `hotspots.py`，全部 `exact` |
| AC-17 | 降级容错三处可见 | DegradedNotice + API + issues |
| AC-18 | 全友覆盖率 ≥60% | 热区推荐全友优先 |
| AC-20 | 绿色环保评估 | 五维之一 |
| AC-21 | 全友价格 + 官网链接 | 热区卡片 |
| AC-22 | 显存安全 | 实测 10/10，峰值 0.968GB |
| AC-25/26 | 降级质量闸门 | 有测试 |
| AC-27 | 图片质量预检，Token=0 | `precheck.py`，断言 `llm.calls == []` |
| AC-28 | AI 图几何一致性自检 | `geo_check.py`，**实测阈值 0.70 太高**（见 10.2） |
| AC-29 | 接口风格一致 | 都是异步 + task_id |
| AC-30 | 全链路 trace_id | contextvars |
| AC-31 | 优雅停机 | `TaskManager.shutdown` |
| AC-33/34 | 降级业务边界 | capabilities 守卫 |
| AC-35 | AI 图热区默认关闭 | `IMAGE_HOTSPOT_ENABLED=False` |
| AC-36 | 语义化进度 ≥3 阶段 | 实测 15%→85%→100% |
| AC-37 | 无公网暴露 | 绑 127.0.0.1 |
| AC-39 | 措辞一致性 | 全文检索无「装修效果图」 |

### ⚠️ 有保留 / 需注意
| AC | 情况 |
|---|---|
| **AC-08** | AI 3D 户型渲染图。**实测达到不了描述**：SD1.5+ControlNet 产出的是**俯视家具平面图**，不是等轴测渲染。**已决定不接 SD1.5**，方案封面改用 3D 场景的等轴测截图。README 已把表述改准 |
| **AC-38** | 出图跨种子稳定 —— 同类问题，未做 |
| **AC-24** | 隐私降级开关 —— 有实现，但**没重测抓包** |

### ❌ 未做
| AC | 内容 | 说明 |
|---|---|---|
| AC-01 | 三角色登录 | 依赖 M1 认证，未开始 |
| AC-10 | 局部替换（inpainting） | 可行性 C，排期最后 |
| AC-11 | **MCP 工具调用** `calc_budget` / `render_layout_svg` | `mcp_servers/` 只有一个 `parse_house_layout.py`；**`render_layout_svg` 现在有真东西可以包了**（`render/svg.py`） |
| AC-12 | Redis 会话恢复 | Redis 没起，未验证 |
| AC-13 | 额度限流 | `redis_client.py` 里有 `QuotaExceeded`，未接 |
| AC-14 | 审计日志 | 未做 |
| AC-15 | 端到端 20/20 | `scripts/e2e_smoke.py` 存在，未跑全 |
| AC-16 | **Docker Compose 4 服务 Healthy** | 未做 |
| AC-19 | 自由选项组合 | 前端有 UI，未测 |
| AC-23 | `/system/metrics` P95 | 未做 |
| AC-32 | Golden Path 可复现 ≥95% | 未做 |

---

## 7. 已完成工作的关键技术决策（含踩过的坑）

这一节是"为什么现在是这样"的答案。**改这些代码前先读这里。**

### 7.1 几何内核 `services/geometry/normalize.py`
把 A-01 的**像素**户型 JSON 归一化成**米制 Scene**。它回答四个有坑的问题：

1. **比例尺** —— A-01 不输出它。用 `px_per_m = sqrt(Σ房间bbox面积_px / total_area_m²)` 推，
   并与尺寸标注交叉校验。**实测两者差 4–8%**：原图本身就不严格按比例，
   所以分歧如实报出，不假装一致。
   ⚠️ **实测有解析结果 `total_area = 0.0`**，会退化到尺寸标注路径。
2. **Y 轴翻转** —— 图片 Y 向下，场景 Y 向上。不翻转的话 3D 是**左右镜像**，而镜像户型看上去完全正常。
3. **原点归位** —— 实测户型从 `[40,40]` 开始，不归位两个渲染器的"居中"对不上。
4. **墙体拓扑** —— 决定 3D 能不能用真实墙体。

**⚠️ 闭合性判据改写过一个严重的 bug（2026-09-23）：**
初版要求「存在一个**首尾重合的折线**」。它在早期一份数据上碰巧成立（那次模型给了 5 点闭环），
于是被当成通用规则。**实测模型大部分时候给的是 4 段独立的碎段**：

```
顶 (95,140)->(1520,140)    底 (95,1190)->(1520,1190)
左 (95,140)->(95,1190)     右 (1520,140)->(1520,1190)
```

四条边严丝合缝围成矩形，但没一段自己是环 → 判 `walls_closed=False` →
`can_build_walls=False` → **3D 直接降级成自由视角，第一人称行走永远起不来**，而界面上一切正常。

**改成问对的问题**：从户型外做**洪水填充**，看能不能渗进任何一间房。
与"模型怎么表示墙"无关。`ENCLOSURE_STEP_M = 0.15`，`ENCLOSURE_MARGIN_M = 1.5`。

### 7.2 可漫游性 `services/geometry/walkable.py`
产出：碰撞线段（**门洞已切开**）、房间净空、门连通图、出生点。

**⚠️ 三个踩过的坑：**
1. **墙是连续的，门只是个符号。** 2D 画门时是用底色在墙上盖出缺口 —— 那只改了画面，没改几何。
   直接拿它做碰撞，**人会被关在房间里出不去**，且不报错。
2. **`CollisionSeg` 有两套端点**：`a`/`b` 是精确碰撞中心线（**不外延**）；
   `render_a`/`render_b` 是 3D 渲染用（**墙角外延半个墙厚、门洞边缘不外延**）。
   直觉的"每端都外延"会把 0.9m 的门挤成 0.7m，而画面上看不出来。
3. **门的两侧判定**：初版沿墙**法向各走 0.6m** 探，实测走廊净宽只有 **0.264m**，
   探针跨过整条走廊打到对面，两侧落进同一间房。更早一版沿**切向**探，症状是自环。
   **改成"沿法向分侧、各取最近的房间"**，与空间宽窄无关。实测可走面积 46% → 100%。

**判据（`MIN_REACHABLE_AREA_RATIO = 0.5`）**：按**面积覆盖率**判，不是按房间数。
一间走不到的阳台不该废掉整个漫游（实测漏一扇推拉门时 88% 的面积仍能走）。

### 7.3 出图：**已决定不接 SD1.5**
实测结论（4 组消融实验）：`controlnet_sd15_canny` 逐边复刻条件图结构，
**条件图俯视则输出必然俯视**。约束强度是刀锋 —— 强了变平面复刻，弱了模型自己编。
方案封面改用 **3D 场景的等轴测截图**（真实几何、确定性、毫秒级、离线）。

**但顺手修了一个真 bug**：`render_conditioning_image` 对 x/y 各用一个缩放因子，
把 685×505 的三居室**强行拉成正方形**（压扁 1.36 倍）。
改成单一缩放因子 + 居中。修后比例 1.359 vs 源 1.356。

### 7.4 2D 渲染 `services/render/`
- **选 SVG 不选位图**：AC-07 的"512×512 **以上**"位图没有"以上"；
  热区要贴住几何（SVG 里房间就是那个 `<polygon>`）；纯字符串拼接**不可能失败**。
- **`Projection` 是渲染与热区共用的唯一一份坐标变换**。热区和画面漂移是 2.2.6 最担心的失败模式。
- **热区从几何推导，不用目标检测**：COCO 80 类里**没有"地板"，没有"墙纸"，甚至没有"空调"**。
- **墙面热区用双环 + `fill-rule="evenodd"` 挖空**：只画外环的话整间房都被墙纸盖住、地板永远悬停不到。
- **天花板热区不在 2D 图上**：俯视图里它和地板投影到完全相同的像素，命中取决于绘制顺序。

**⚠️ 修过一个 XML 注入 bug**：`xml.sax.saxutils.escape` 默认**只转 `& < >`，不转引号**。
房间名是模型读图读出来的，会进 `aria-label="..."`。
实测 `客厅" onload="alert(1)` 会让 `aria-label` 被截成 `客厅`，`onload` 成为**独立属性** ——
**而文档照样解析成功**，所以"是合法 XML"这种断言抓不到它。

### 7.5 3D 漫游 `frontend/src/three/` + `components/SceneViewer.vue`
- **前端一行几何都不推导**：墙体用 `walkable.collision` 的 `render_*`（门洞已切、墙角已外延），
  所以"看得见的门"和"走得过去的门"天生同一份数据。
- **`coords.ts` 的映射**：图纸 y 向上 → Three 地面 XZ，所以 `planToEngine(x,y) = (x, h, -y)`。
  映射成 +z 会**镜像**，而镜像户型看上去完全正常。
- **镜像的自检**：把玩家位置**同时**画在 3D 里和后端渲染的 2D 户型 SVG 上（小地图）。
  两套**互相独立**的计算指向同一位置，写反时绿点跑到另一侧。
  诊断面板会显示 `✓ 坐标映射自检通过`。
- **相机 FOV**：`PerspectiveCamera.fov` 是**垂直**视场角。
  初版写 68（心里想的是水平）→ 换算水平 ≈98° 超广角 → **东西看起来更小更远**，
  用户反馈"房间都很小"就是这个。现在按水平 78° 定值、用宽高比反算垂直，HUD 可调（50–100°）。
- **门**：后端给 `hinge`/`along`/`normal`；前端建门扇 + **门洞上方过梁**（不加过梁门洞通到天花板，不像门）。
  **默认全开**。**F 键**开关（切换行走/自由视角改成 **G**）。
  关门时补一段碰撞 —— 否则"关门"只是视觉动作。
  转向符号 `θ=atan2(ay,ax)` 是推出来的：写反了对**对称墙盒**看不出来，对门扇是致命的。
- **碰撞**：点到线段距离；**分轴尝试 → 撞墙沿墙滑行**（停住像卡进几何里）；
  按 5cm 子步切分（帧率掉到 5fps 时单帧位移 50cm，正好穿墙）；
  单帧 dt 封顶 0.1s（否则切走标签页再回来一帧被瞬移到户外）。
- **硬件守卫**：DPR 封顶（1.5/1.0/0.75 三档）；连续 3 秒 <30fps 自动降一档画质并**明说降了档**；
  界面显示实时帧率与**实际使用的显卡**；检测到软件渲染会提示去开硬件加速；
  **刻意不开阴影**（室内光照平、收益有限而开销大）。

### 7.6 前端数据不丢（刚修）
用户反馈：解析期间点开别的页面，进度全没、要重新上传；解析完成后离开再回来结果也没了。

**成因**：状态只在内存里 —— 轮询状态是组件内 ref（卸载即销毁）；`task_id` 只在地址栏 query，
而从侧栏点回「户型解析」走的是 `router.push('/parse')`，**query 没了**。
任务其实一直在后端跑（结果留 1 小时），是前端把它忘了。

**修法**（`useTaskPolling(sessionKey)`）：三级兜底 —— 地址栏 query → 模块级会话表 → `sessionStorage`。
**只存 task_id 不存结果**（结果上百 KB，sessionStorage 有配额风险）。
同一个任务重复 `start()` 时**不清空阶段轨迹**。

### 7.7 修过的"说谎"类 bug
- `prechecking` 阶段是任务启动时无条件写死的，而代码里**没有任何图片质检实现** —— 界面在撒谎。
  改成真实节点 `precheck_image`（AC-27）。
- 报价单审查时界面显示「正在生成装修方案…」（节点名 → 阶段映射没带任务类型）。
- `TaskRecord.progress` **从来没被赋值过**，Redis 挂了（常态）就永远显示 0。
- 失败会把 phase 落成 `done` → `PHASE_TEXT['done']` = "完成" → **大红叉配"完成"**。

### 7.8 其它已修
- 三个 Agent 写了 `Phase` 枚举外的值（`parsed`/`diagnosed`/`reviewed`），前端看到英文原文
- `pytest.ini` 用 cp936 读，**一个中文就让 pytest 在解析配置阶段崩** → 改纯 ASCII
- `requests` 的 `trust_env=True` 会读 Windows 系统代理 → `_SESSION.trust_env = False`
- scripts 里 `print` 带 `⚠️` 在 cp936 控制台崩 → 显式 `sys.stdout.reconfigure(encoding="utf-8")`

---

## 8. 未完成 / 推荐的下一步

按依赖与价值排序：

1. **Docker Compose（AC-16）** —— 4 服务 Healthy。独立、验收明确
2. **`/system/metrics`（AC-23）** —— P95 基线。埋点已有基础（`x-elapsed-ms` 响应头）
3. **审计日志（AC-14）** —— 记录登录/解析/生成，含 `degraded` 字段，可查询
4. **MCP 工具（AC-11）** —— `calc_budget` + `render_layout_svg`。
   ⚠️ 后者现在有真东西可包了（`services/render/svg.py`），以前没法做
5. **登录与角色（AC-01）** —— admin/designer/user
6. **DB 持久化** —— 现在户型存内存 + Redis，Redis 没起时重启即失
7. **前端测试** —— **目前前端零测试覆盖**（后端 702 个）。
   几何逻辑已经尽量放后端了，但 3D 漫游、热区交互没有自动化测试
8. **标定 AC-28 的 IoU 阈值** —— 需要 ≥20 张正常样本（见 10.2）

### 可以讲但没做的
- 3D 场景的家具摆放（现在只有空房间）
- 3D 场景的材质/贴图（现在是纯色）
- 方案封面用 3D 等轴测截图（AC-08 的替代方案，已决定但未实施）

---

## 9. 当前会话（最近一轮）做了什么

> 这一节是"上一个窗口刚刚干完的事"，避免重复劳动。

### 9.1 用户这一轮提的三个问题
1. **3D 尺寸不满意** —— "第一视角感觉房间都很小"
2. **门没做出来** —— 希望像游戏一样按 F 开关门，默认全开
3. **离开页面丢任务** —— 进度消失、结果丢失

### 9.2 处理过程与结论

**① 房间显小**
- **先量了几何**：模型算出主卧 3.99×3.10m，图纸是 4.0×3.2m → **尺寸是对的**
- 真因：Three.js `PerspectiveCamera.fov` 是**垂直**视场角，我写 68 以为是水平 →
  换算水平 ≈98° 超广角 → 东西显小显远
- 改：按水平视场角定值（默认 78°）+ 宽高比反算垂直 + HUD 可调（50–100°，存 localStorage）
- **踩坑**：从 `rig.ts` 静态引 `DEFAULT_HFOV_DEG` 把 three 拉回主包，ParseView 45KB→437KB。
  改放 `coords.ts`（不 import three）才修好

**② 门** —— 全部实现，见 7.5。实测门扇中点与模型报的门中心差 **3–8cm**
（模型给的门位置本来就略偏墙线，后端用"投影到墙上"的点，因为**门必须开在墙上**）

**③ 丢任务** —— 全部实现，见 7.6

### 9.3 同一轮更早还做了
- **自造演示户型图**：`演示素材/户型图/`（3 张 PNG + README + `scripts/make_demo_floorplans.py`）
  - **画成双线墙**（不是一条粗线）：初版在窗的位置**用底色盖出缺口**当窗，
    结果外墙被切成 12 段，模型照实读出"墙是断的" → 3D 降级。
    **建筑制图里窗本来就不打断墙，只有门打断**
  - `02` 号图改成**走廊式星形连通**（每个房间的门直接开向走廊）：
    链式连通只要模型漏检一扇门，后面的房间全成孤岛
- **修闭合性判据**（见 7.1）、**修门侧判定**（见 7.2）
- **修进度条永远为 0**、**修失败显示"完成"**、**进度卡移到右栏**
- **熔断放宽**：A-01 解析 45s → **90s**，A-02 诊断 30s → **60s**
  （实测连续 5 次真实解析有 2 次撞 45s，而成功的那几次整条链就要 40–62s）

### 9.4 这一轮结束时服务状态
```
前端   http://localhost:5173  → HTTP 200（vite dev，后台 shell）
后端   http://127.0.0.1:8000  → degraded（redis: False，其它 OK，后台 shell）
测试   702 passed, 1 deselected, ~35s
提交   cb629af，已推 Gitee；GitHub 网络不通
```

---

## 10. 已知不确定性 / 风险（**必须如实转达用户**）

### 10.1 视觉模型的输出波动（上游，非几何层问题）
- **门检出率不是 100%**。同一张图连跑：好的时候 9 扇全中（可走面积 100%），
  差的时候漏一两扇（可走面积 46%）。界面上会把逛不到的房间标灰并写明。
- **`01` 号演示图实测有一次走降级路径**（`degraded_basic`，只有房间名没坐标）。
  **只跑过一次，不能断定每次都这样** —— 但演示建议用 `02` 或 `03`。
- **`total_area` 有时是 0**，会退化到尺寸标注路径推比例尺。

### 10.2 AC-28 的 IoU 阈值**实测证实了文档自己的担心**
需求文档 2.2.6 预设「IoU < 0.70 就不给热区」，而「坑 19」自己承认那是拍的。
用 `scripts/calibrate_hotspot_iou.py` 在 `E:/quanyou/outputs` 上实测：

```
A_thickEdge_g1.0_s6 （消融里"人工判定最好"的那张）  0.667
B_thinLine ×3                                      0.216
预设 0.70 → 达标 0/4
```

**担心被证实。** 但 n=4 且其中 3 张是故意做坏的对照组 ——
它只能证明"0.70 定高了"，**不能**用来定新阈值（标定规程要求 ≥20 张正常样本）。
所以 `IMAGE_HOTSPOT_ENABLED` 保持 `False`。

### 10.3 熔断余量
已放宽到 90s/60s，但**没有重跑足够多次验证**。如果还偶发超时，可以再放宽
（上限参考 `LLM_TIMEOUT_SECONDS = 120`）。

### 10.4 我（上一个窗口）**看不到的东西**
- **WebGL 实时画面看不到**（Read 支持 PNG，但渲染出来的是实时画面）。
  3D 的观感、开门手感、帧率**必须让用户确认**。
- **SVG 看不了**（Read 支持 PNG/JPG，不支持 SVG）。
  2D 矢量图的观感也只能靠用户看（或先转成 PNG）。
- **PNG 能看** —— 这一点我一度搞错，还让用户白帮忙看了几次图。**新窗口别再犯。**

---

## 11. 常用命令

### 11.1 测试（**改完代码必跑**）
```bash
cd "C:/Users/DELL/Desktop/全友·智绘家QuanYou Smart HomeCanvas"
python -m pytest -q                    # 全量，~35s，702 个
python -m pytest tests/test_geometry.py -q
python -m pytest -q -k "关键字"
```

### 11.2 起服务
```bash
# 后端（base 环境即可）
python -m uvicorn backend.app.main:app --port 8000 --host 127.0.0.1

# 前端
cd frontend && npm run dev          # → http://localhost:5173
cd frontend && npm run typecheck    # vue-tsc
cd frontend && npm run build        # ⚠️ 构建后检查 ParseView 是否还在 ~45KB
```

### 11.3 脚本
```bash
python scripts/make_demo_floorplans.py      # 生成演示户型图
python scripts/render_plan.py               # 户型 JSON → SVG（含热区）
python scripts/calibrate_hotspot_iou.py E:/quanyou/outputs   # IoU 标定
python scripts/e2e_smoke.py                 # 端到端
python scripts/warmup.py                    # 模型预热
python scripts/bench_image.py               # 出图基准（要 pytorch 环境）
python scripts/ingest_knowledge.py          # 知识库入库
```

### 11.4 接口
```
POST /api/v1/layout/parse                异步，→ task_id
POST /api/v1/design/generate             异步
POST /api/v1/avoid-pit/review            异步
GET  /api/v1/task/{id}/status            轮询（200 + phase/progress/phase_text）
GET  /api/v1/material/price              同步
GET  /api/v1/layout/{id}/plan.svg        同步，**裸 image/svg+xml 不套信封**
GET  /api/v1/layout/{id}/hotspots        同步
GET  /api/v1/layout/{id}/walkable        同步，3D 漫游几何
GET  /api/v1/system/health               同步，永远 200
```
**约定**：业务失败走 **HTTP 200 + code != 0**（4.4 明确要求），不用 4xx。

### 11.5 输出目录
```
E:/quanyou/outputs/     出图基准、消融实验、方案 SVG
E:/quanyou/models/      SD1.5 / ControlNet / LCM-LoRA
E:/quanyou/uploads/     上传
```
用户桌面上的 `ui参考/` 里有 design 参考（`stitch_ai/botanical_warmth_natural_living/DESIGN.md`）。

---

## 12. 与用户协作的注意事项

1. **用户是中文母语，全程中文回复。**
2. **用户在准备面试**，这个项目是他的作品集。所以：
   - 每个技术决策要能讲出"为什么"
   - 主动解释权衡（他已经多次追问过细节）
   - 诚实 > 好看。他明确认可"承认降级/承认做不到"的做法
3. **用户会亲自验证**。他会真的上传图、看界面、提反馈。
   所以**不能糊弄** —— 他会在下一轮把你糊弄的地方指出来。
4. **用户的反馈往往是准确的**，即使表述是感受式的（"感觉房间都很小"）。
   先量，往往会发现他指出的方向是对的，只是原因和他想的不一样。
5. **他会要求"可实现性第一"** —— 任何方案都要先确认在这台机器上跑得通。
6. 停下来问的时机：方案取舍、需要人眼确认、有风险。

---

## 13. 快速自检：接手后你应该能回答

- [ ] 项目是做什么的？给谁用的？
- [ ] 有哪些绝对不能碰的红线？（版权、需求文档、API key、措辞）
- [ ] 现在的测试数是多少？怎么跑？
- [ ] 为什么 three.js 必须动态 import？
- [ ] 为什么 `walls_closed` 用洪水填充判而不是"有闭环"？
- [ ] 为什么 3D 的门要分 `a/b` 和 `render_a/render_b` 两套端点？
- [ ] 为什么"房间显小"是相机问题不是几何问题？
- [ ] 前端为什么会丢任务？怎么修的？
- [ ] AC-28 的阈值实测结论是什么？
- [ ] 哪些 AC 还没做？
- [ ] 哪两件事我看不到、必须让用户确认？

---

*生成时间：2026-09-23*
*对应提交：`cb629af`*
